console.log("hello" + name)

var age = 25 + 5;

/*take input from the user and assign it to the variable message*/
var message = window.prompt('hello');

window.alert('you entered ' + message);

console.log(age);

/*array.shift - removes the first element of the array
array.unshift - adds something to the first element
array.sort - arranges teams in alphabetical or numerical order
array.push - push adds in element at the end of the array
array.pop - removes the element at the end of the array
array. reverse - reverses order
can do multiple methods with . chaining */
