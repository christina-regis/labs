//Define a new module.  The first argument  is what we want to call our app, the second is an array of dependencies (which we don't need at the moement, so there are none)
//name of module- creating a module
angular.module('IntroToAngularApp', []);
